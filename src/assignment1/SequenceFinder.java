package assignment1;

import java.util.*;

public class SequenceFinder {

    /**
     * @require Configuration.isValid(configuration) && newFacility!= null &&
     * !Configuration.installedFacilities(configuration).contains(newFacility)
     * @ensure Returns a cheapest installation sequence for the new Facility, if there is
     * one, or null otherwise.
     */
    public static List<Action> findSequence(HashMap<Site, HashSet<Facility>> configuration,
                                            Facility newFacility) {
        return null; // REMOVE THIS LINE AND IMPLEMENT THIS METHOD
    }

}
